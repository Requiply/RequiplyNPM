{
  "name": "requiply-npm",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:api-sdk": "npm run build --workspace @requiply/api-sdk",
    "test:api-sdk": "npm run test --workspace @requiply/api-sdk"
  }
}
